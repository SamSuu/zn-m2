name: zn-m2 build

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "zn-m2/basic"
    paths:
      - ".github/workflows/zn-m2.yml"
      - "configs/zn-m2-basic.config"

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android /opt/hostedtoolcache/CodeQL || true
          docker image prune -af || true
          df -h

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
            bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext \
            gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 \
            libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev \
            libncurses5-dev libncursesw5-dev libreadline-dev libssl-dev libtool lrzsz \
            mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip \
            libpython3-dev qemu-utils rsync scons squashfs-tools subversion swig texinfo \
            uglifyjs unzip vim wget xmlto xxd zlib1g-dev

      - name: Clone source
        run: |
          git clone --depth 1 https://github.com/LiBwrt/openwrt-6.x.git openwrt

      - name: Update feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Load config
        run: |
          cp configs/zn-m2-basic.config openwrt/.config
          cd openwrt
          make defconfig
          cp .config "$GITHUB_WORKSPACE/final.config"
          echo "===== TARGET ====="
          grep -E '^CONFIG_TARGET_|^CONFIG_TARGET_PROFILE=' .config || true
          echo "===== CORE PACKAGES ====="
          grep -E '^CONFIG_PACKAGE_(mwan3|luci-app-mwan3|zerotier|kmod-tun|nss-firmware|kmod-qca-nss-dp|kmod-qca-nss-ecm)=' .config || true
          echo "===== USB CHECK ====="
          grep -E '^(CONFIG_DEFAULT_|CONFIG_PACKAGE_)(kmod-usb|kmod-phy-qcom|kmod-usb-phy|usbutils)' .config || true
          echo "===== WIFI CHECK ====="
          grep -E '^(CONFIG_DEFAULT_|CONFIG_PACKAGE_)(kmod-ath11k|ath11k-firmware|wpad|hostapd|iwinfo|iw|wireless-regdb|cypress-nvram|kmod-cfg80211|kmod-mac80211)' .config || true

      - name: Download sources
        run: |
          cd openwrt
          make download -j8 || make download -j1 V=s

      - name: Compile the firmware
        run: |
          cd openwrt
          make -j"$(nproc)" || make -j1 V=s

      - name: Set artifact name
        id: vars
        run: |
          echo "DATE=$(date +%Y%m%d-%H%M)" >> "$GITHUB_OUTPUT"

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: zn-m2-openwrt-${{ steps.vars.outputs.DATE }}
          path: |
            openwrt/bin/targets/**/*
            final.config
          retention-days: 14
